language: cpp
compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq qt4-dev-tools g++

script: 
  - bash ./UnitTest/Travis/make.sh
  - ./UnitTest/ZeroMSUnitTestTest